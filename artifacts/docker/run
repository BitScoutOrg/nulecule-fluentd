docker run -d -p $syslog_listen_port:$syslog_listen_port/udp -u $uid $link_es $link_qpid -e QP_HOST=$qpidhost -e ES_HOST=$elasticsearchhost -e SYSLOG_LISTEN_PORT=$syslog_listen_port --name $appname $testbindmountopt ${testhostdir}$testcontainerdir $image
